<?php

class OrdencomprasTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('ordencompras')->truncate();

		$ordencompras = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('ordencompras')->insert($ordencompras);
	}

}
