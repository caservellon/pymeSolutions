<?php

class ValorcampolocalcrmsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('valorcampolocalcrms')->truncate();

		$valorcampolocalcrms = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('valorcampolocalcrms')->insert($valorcampolocalcrms);
	}

}
