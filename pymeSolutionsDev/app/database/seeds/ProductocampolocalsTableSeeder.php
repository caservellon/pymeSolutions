<?php

class ProductocampolocalsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('productocampolocals')->truncate();

		$productocampolocals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('productocampolocals')->insert($productocampolocals);
	}

}
