<?php

class ProveedorcampolocalsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('proveedorcampolocals')->truncate();

		$proveedorcampolocals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('proveedorcampolocals')->insert($proveedorcampolocals);
	}

}
