<?php

class ValorcampolocalsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('valorcampolocals')->truncate();

		$valorcampolocals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('valorcampolocals')->insert($valorcampolocals);
	}

}
