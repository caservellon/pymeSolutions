<?php

class DetalleasientosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('detalleasientos')->truncate();

		$detalleasientos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('detalleasientos')->insert($detalleasientos);
	}

}
