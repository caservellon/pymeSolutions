<?php

class CampolocalsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('campolocals')->truncate();

		$campolocals = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('campolocals')->insert($campolocals);
	}

}
