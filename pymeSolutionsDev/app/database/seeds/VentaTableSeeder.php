<?php

class VentaTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('venta')->truncate();

		$venta = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('venta')->insert($venta);
	}

}
