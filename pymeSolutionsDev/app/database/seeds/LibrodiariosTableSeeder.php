<?php

class LibrodiariosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('librodiarios')->truncate();

		$librodiarios = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('librodiarios')->insert($librodiarios);
	}

}
